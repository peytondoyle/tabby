# Tabby is retired.
# The production app now serves a static shutdown page and APIs return 410.
#
# Keep this file as a non-secret placeholder only. Do not add live Supabase,
# Clerk, OpenAI, R2, or Redis credentials back to this project.
