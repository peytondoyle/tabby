# ⚠️ IMPORTANT: Copy this file to .env and replace with your actual values
# ⚠️ NEVER commit your real .env file to version control!

# Supabase Configuration
# Get these values from your Supabase dashboard at https://supabase.com/dashboard/project/[PROJECT_ID]/settings/api

# Your Supabase project URL
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase Publishable key (safe for client-side code)
# ⚠️ MUST start with "sb_publishable_" - legacy JWT keys are NOT supported
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your-key-here

# Your Supabase Secret key (server-side only - NEVER expose to client!)
# ⚠️ MUST start with "sb_secret_" - NEVER commit or expose this key
SUPABASE_SECRET_KEY=sb_secret_your-secret-key-here

# API Configuration for Development
# For local vercel dev (default, same origin - leave empty)
VITE_API_BASE=

# For Vite-only dev (optional, proxy or direct to production)
# VITE_API_BASE=https://your-app.vercel.app

# Database password for CLI operations (KEEP SECURE - never commit this!)
SUPABASE_DB_PASSWORD=your-secure-db-password

# Project reference ID for CLI
SUPABASE_PROJECT_REF=your-project-ref

# OCR API Keys (for production receipt scanning)
# GOOGLE_VISION_API_KEY=your-google-vision-api-key
# AWS_TEXTRACT_ENABLED=true
# OCR_ENABLED=true