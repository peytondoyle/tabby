# CORS Configuration for local development
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional: Allow all origins with wildcard (use only in dev)
# CORS_ALLOW_ALL=1

# Vite API Base for absolute URLs (bypasses Vite proxy)
VITE_API_BASE=http://127.0.0.1:3000

# Show dev banner with API health status (dev only)
VITE_SHOW_DEV_BANNER=1

# Log API errors to Supabase (optional, enabled by default in dev)
# VITE_LOG_API_ERRORS=1

# Supabase Configuration (optional)
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key
# SUPABASE_SECRET_KEY=your-secret-key